# Maestro E2E flows

Run all:

```bash
maestro test .maestro
```

Individual flow:

```bash
maestro test .maestro/welcome_to_value.yaml
```

Prereq: the Expo dev client (or a release build) must be running with `appId: com.upmind.app`.
